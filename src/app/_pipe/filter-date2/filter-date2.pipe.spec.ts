import { FilterDate2Pipe } from './filter-date2.pipe';

describe('FilterDate2Pipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDate2Pipe();
    expect(pipe).toBeTruthy();
  });
});
